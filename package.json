{
  "name": "@chickendinosaur/url",
  "version": "1.0.0",
  "description": "URL parsing.",
  "main": "lib/url.js",
  "files": [
    "lib/"
  ],
  "scripts": {
    "benchmark": "npm run test && node benchmark/parse.js && node benchmark/query-string/parse.js && node benchmark/query-string/format.js",
    "benchmark-parse": "npm run test && node benchmark/parse.js",
    "benchmark-query-string-parse": "npm run test && node benchmark/query-string/parse.js",
    "benchmark-query-string-stringify": "npm run test && node benchmark/query-string/stringify.js",
    "build": "npm run clean && cp -rf src/* lib/",
    "clean": "rm -rf lib/*",
    "prepublish": "npm run test",
    "test": "npm run build && tap test/**/*.tap.js --reporter spec"
  },
  "author": {
    "name": "John Pittman",
    "email": "john@chickendinosaur.com",
    "url": "https://github.com/chickendinosaur"
  },
  "license": "MIT",
  "devDependencies": {
    "benchmark": "^2.1.2",
    "tap": "^10.0.2"
  },
  "keywords": [
    "parsing",
    "querystring",
    "url"
  ],
  "private": false,
  "repository": {
    "type": "git",
    "url": "git+https://github.com/chickendinosaur/url.git"
  },
  "bugs": {
    "url": "https://github.com/chickendinosaur/url/issues"
  },
  "homepage": "https://github.com/chickendinosaur/url#readme",
  "generator-module": {
    "global": false
  }
}
