{
  "name": "",
  "version": "0.0.1",
  "description": "",
  "main": "lib/index.js",
  "files": [
    "lib/"
  ],
  "scripts": {
    "benchmark": "npm run test && npm run benchmark-url-parser && npm run benchmark-fast-url-parser && npm run benchmark-url",
    "benchmark-url-parser": "node benchmark/url-parser.js",
    "benchmark-fast-url-parser": "node benchmark/fast-url-parser.js",
    "benchmark-url": "node benchmark/url.js",
    "build": "npm run clean && babel src --out-dir lib",
    "clean": "rm -rf lib/*",
    "prepubish": "npm run test && npm access public",
    "test": "npm run build && tape test/unit/**/*.js | tap-spec"
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "benchmark": "^2.1.2",
    "fast-url-parser": "^1.1.3",
    "happiness-format": "^1.1.0",
    "tap-spec": "^4.1.1",
    "tape": "^4.6.0",
    "url": "^0.11.0"
  }
}
